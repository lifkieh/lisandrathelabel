<span class="product-list-text" {{ block.shopify_attributes }}>
  {{ block.settings.text }}
</span>

{% schema %}
{
  "name": "t:names.text",
  "tag": null,
  "settings": [
    {
      "type": "richtext",
      "id": "text",
      "label": "t:settings.text",
      "default": "t:text_defaults.collection_description"
    }
  ]
}
{% endschema %}
